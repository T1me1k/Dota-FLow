import test from 'node:test';import assert from 'node:assert/strict';import {VoiceCoachCoordinator,formatCoachCallVoiceMessage} from '../src/voice-coach.mjs';
test('voice consumes one coach call, deduplicates, and critical interrupts',()=>{const v=new VoiceCoachCoordinator({cooldownSec:20}),state={phase:'playing',gameTimeSec:1};const low={primaryAction:'FARM',urgency:'LOW',reasons:['farm']};assert.match(formatCoachCallVoiceMessage(low),/farm/);assert.ok(v.update({state,coachCall:low}));assert.equal(v.update({state:{...state,gameTimeSec:2},coachCall:low}),null);assert.ok(v.update({state:{...state,gameTimeSec:3},coachCall:{primaryAction:'RESET',urgency:'CRITICAL',reasons:['low hp']}}));});
