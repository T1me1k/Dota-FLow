import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import{replayCapture}from'../src/runtime-v023.mjs';test('replay deterministic and immutable',async()=>{const p='fixtures/captures/synthetic-v023/canonical-events.jsonl',before=await readFile(p,'utf8');assert.deepEqual(await replayCapture('fixtures/captures/synthetic-v023'),await replayCapture('fixtures/captures/synthetic-v023'));assert.equal(await readFile(p,'utf8'),before)})
