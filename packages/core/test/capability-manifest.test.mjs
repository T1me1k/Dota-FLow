import test from'node:test';import assert from'node:assert/strict';import{GEP_FEATURE_MANIFEST,negotiateCapabilities}from'../src/runtime-v023.mjs';test('single versioned manifest negotiates intersection',()=>{const x=negotiateCapabilities(['game_info']);assert.equal(GEP_FEATURE_MANIFEST.manifestVersion,1);assert.deepEqual(x.supportedFeatures,['game_info']);assert.ok(x.unavailableFeatures.includes('items'))})
