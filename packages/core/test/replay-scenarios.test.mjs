import test from'node:test';import assert from'node:assert/strict';import{listReplayScenarios,runReplaySuite}from'../src/replay-calibration.mjs';test('all 30 replay scenarios and 90 checkpoints pass',async()=>{const s=await listReplayScenarios(),r=await runReplaySuite();assert.equal(s.length,30);assert.equal(r.summary.checkpoints,90);assert.equal(r.summary.failed,0);assert.equal(r.summary.replayCompletionRate,1)});
