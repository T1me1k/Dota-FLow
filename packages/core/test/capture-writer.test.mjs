import test from'node:test';import assert from'node:assert/strict';import{mkdtemp,readFile,rm}from'node:fs/promises';import{tmpdir}from'node:os';import{join}from'node:path';import{CaptureSessionWriter}from'../src/runtime-v023.mjs';test('append-only writer flushes and deduplicates snapshots',async()=>{const root=await mkdtemp(join(tmpdir(),'df-'));const w=new CaptureSessionWriter(root);await w.start({sessionId:'x'});await w.raw({a:1});await w.snapshot({a:1});assert.equal(await w.snapshot({a:1}),null);const s=await w.stop();assert.equal(s.flushed,true);assert.match(await readFile(join(w.directory,'raw-gep.jsonl'),'utf8'),/"sequence":1/);await rm(root,{recursive:true})})
