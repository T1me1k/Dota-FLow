import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('live match view model',async()=>{const source=await readFile(new URL('../../../apps/desktop/src/main.tsx',import.meta.url),'utf8');assert.match(source,/NEXT MOVE/);assert.match(source,/Why now/);assert.match(source,/Cancel when/)});
