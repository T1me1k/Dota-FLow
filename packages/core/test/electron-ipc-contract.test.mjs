import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('preload exposes allowlisted API without raw ipc',async()=>{const s=await readFile('apps/overwolf-electron/src/preload/preload.ts','utf8');assert.match(s,/contextBridge/);assert.doesNotMatch(s,/exposeInMainWorld\([^]*ipcRenderer\s*[,}]/);assert.match(s,/runtime:get-snapshot/);assert.match(s,/removeListener/)})
