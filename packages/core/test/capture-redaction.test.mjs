import test from'node:test';import assert from'node:assert/strict';import{mkdtemp,mkdir,writeFile,readFile,rm}from'node:fs/promises';import{tmpdir}from'node:os';import{join}from'node:path';import{redactCapture}from'../src/runtime-v023.mjs';test('redaction removes identifiers',async()=>{const r=await mkdtemp(join(tmpdir(),'df-')),i=join(r,'in'),o=join(r,'out');await mkdir(i);await writeFile(join(i,'x.json'),JSON.stringify({steamId:'76561191234567890',playerName:'Alice',note:'ok',local:'C:\\Users\\Alice\\x'}));await redactCapture(i,o);const text=await readFile(join(o,'x.json'),'utf8');assert.doesNotMatch(text,/Alice|76561191234567890/);assert.match(text,/REDACTED_PATH/);await rm(r,{recursive:true})})
