import test from'node:test';import assert from'node:assert/strict';import{listReplayScenarios,runReplayCalibration,parseCanonicalRecording}from'../src/replay-calibration.mjs';test('rerun is deterministic, isolated and fixtures immutable',async()=>{const [a,b]=await listReplayScenarios();const fixture=await parseCanonicalRecording(a.recordingPath),before=JSON.stringify(fixture);const x=await runReplayCalibration(a);await runReplayCalibration(b);const y=await runReplayCalibration(a);assert.deepEqual(x.finalCall,y.finalCall);assert.equal(JSON.stringify(fixture),before);assert.ok(x.historySize<=120)});test('malformed fixture gives diagnostic error',async()=>{await assert.rejects(()=>parseCanonicalRecording('fixtures/scenarios/replay-scenarios.json'),/Malformed replay.*line [12]/)})
