import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('workbench model provides filters and structured detail',async()=>{const s=await readFile(new URL('../../../apps/desktop/src/runtime/scenario-workbench-model.ts',import.meta.url),'utf8');for(const x of['mode','role','heroId','phase','passed','search','timeline','goldenDiff','strategyTrace','reviewMetrics'])assert.match(s,new RegExp(x))});
