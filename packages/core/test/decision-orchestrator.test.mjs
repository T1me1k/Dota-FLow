import test from 'node:test'; import assert from 'node:assert/strict';
import { orchestrateDecision, formatCoachCall } from '../src/decision-orchestrator.mjs';
test('orchestrator returns complete serializable bounded-confidence contract',()=>{const c=orchestrateDecision({state:{phase:'playing',gameTimeSec:10,health:900,maxHealth:1000},macroDecision:{action:'FARM',confidence:.7,reasons:['economy']},dataQuality:{status:'LIVE'}}); for(const k of ['primaryAction','primaryDomain','title','instruction','confidence','urgency','reasons','steps','secondaryActions','suppressedActions','conflicts','blockers','missingSignals','cancellationConditions','expiresAtSec','nextEvaluationSec','dataQuality','strategyTrace','generatedAtSec'])assert.ok(k in c);assert.ok(c.confidence>=0&&c.confidence<=1);assert.doesNotThrow(()=>JSON.stringify(c));});
test('critical health overrides objective and display mode does not alter decision',()=>{const c=orchestrateDecision({state:{gameTimeSec:4,health:18,maxHealth:100},objectiveDecision:{action:'TAKE_ROSHAN',confidence:.95}});assert.equal(c.primaryAction,'RESET_BEFORE_OBJECTIVE');assert.match(formatCoachCall(c,'BEGINNER'),/Главная/);assert.equal(c.primaryAction,'RESET_BEFORE_OBJECTIVE');});
