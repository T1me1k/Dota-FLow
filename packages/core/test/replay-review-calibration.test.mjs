import test from'node:test';import assert from'node:assert/strict';import{runReplaySuite}from'../src/replay-calibration.mjs';test('each replay produces review with zero contradictory calls',async()=>{for(const r of(await runReplaySuite()).results){assert.equal(r.reviewMetrics.contradictoryCallRate,0);assert.ok(Array.isArray(r.review.timeline));assert.doesNotThrow(()=>JSON.stringify(r.review))}});
