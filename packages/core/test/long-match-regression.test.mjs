import test from'node:test';import assert from'node:assert/strict';import{listReplayScenarios,runReplayCalibration}from'../src/replay-calibration.mjs';test('three long matches retain bounded histories and phases',async()=>{const xs=(await listReplayScenarios()).filter(x=>['carry-01-anti_mage','mid-01-puck','hard-support-01-crystal_maiden'].includes(x.id));for(const x of xs){const r=await runReplayCalibration(x);assert.ok(r.timeline.at(-1).gameTimeSec>=2160);assert.ok(r.historySize<=120)}});
