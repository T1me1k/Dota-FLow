import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('desktop runtime provider',async()=>{const source=await readFile(new URL('../../../apps/desktop/src/runtime/provider.ts',import.meta.url),'utf8');assert.match(source,/MockRuntimeProvider/);assert.match(source,/ReplayRuntimeProvider/);assert.match(source,/ElectronIpcRuntimeProvider/)});
