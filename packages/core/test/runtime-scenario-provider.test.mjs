import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('providers expose scenario capabilities and electron fails closed',async()=>{const s=await readFile(new URL('../../../apps/desktop/src/runtime/provider.ts',import.meta.url),'utf8');for(const x of['listScenarios','runScenario','runScenarioCategory','loadReplayScenario','getScenarioGoldenDiff','failed closed'])assert.match(s,new RegExp(x))});
