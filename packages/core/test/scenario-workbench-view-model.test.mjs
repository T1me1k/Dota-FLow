import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('scenario workbench view model',async()=>{const source=await readFile(new URL('../../../apps/desktop/src/ScenarioWorkbench.tsx',import.meta.url),'utf8');assert.match(source,/Checkpoint timeline/);assert.match(source,/Run visible/);assert.match(source,/Golden diff/)});
