import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';for(const h of['anti_mage','faceless_void','medusa','phantom_assassin','luna','juggernaut'])test(`hero pack ${h}`,async()=>{const m=JSON.parse(await readFile(new URL(`../../../fixtures/scenarios/heroes/${h}/manifest.json`,import.meta.url)));assert.equal(m.heroId,h);assert.ok(m.scenarios.length);assert.equal(m.profileTier,'DETAILED')});
