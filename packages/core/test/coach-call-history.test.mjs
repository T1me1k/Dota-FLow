import test from 'node:test';import assert from 'node:assert/strict';import {DecisionOrchestratorCoordinator} from '../src/decision-orchestrator.mjs';
test('history deduplicates, is bounded, expires and emergency overrides',()=>{const x=new DecisionOrchestratorCoordinator({historyLimit:3});for(let i=0;i<10;i++)x.update({state:{gameTimeSec:i,health:100,maxHealth:100},macroDecision:{action:'FARM',confidence:.8}});assert.equal(x.history.length,1);x.update({state:{gameTimeSec:10,health:10,maxHealth:100},macroDecision:{action:'FARM'}});assert.match(x.current.primaryAction,/RESET/);assert.ok(x.history.length<=3);});
