import fs from 'node:fs';import {importReviewJsonl} from '../packages/core/src/index.mjs';const text=fs.readFileSync('fixtures/recordings/sample-gep-session.jsonl','utf8');const {review,errors}=importReviewJsonl(text);console.log(`Match review: FPI ${review.flowPerformanceIndex}; timeline ${review.timeline.length}; parse errors ${errors.length}`);console.log(JSON.stringify(review.metrics,null,2));
