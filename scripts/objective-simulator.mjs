import {evaluateObjective,createInitialGameState} from '../packages/core/src/index.mjs';
for(const [name,objectiveContext] of [['successful Roshan',{objective:'ROSHAN',dataQuality:'MANUAL',roshanAvailable:true,enemyCoreDead:true,teamReady:4,visibleEnemies:2,objectiveDamage:.8}],['unsafe Roshan',{objective:'ROSHAN',dataQuality:'STALE',teamReady:null,visibleEnemies:null}],['objective trade',{objective:'TOWER',dataQuality:'MANUAL',enemyCoreDead:true,teamReady:3,visibleEnemies:3}]]){const d=evaluateObjective(createInitialGameState({gameTimeSec:1400,objectiveContext}));console.log(`${name}: ${d.action} ${(d.confidence*100).toFixed(0)}% [${d.dataQuality}]`);}
