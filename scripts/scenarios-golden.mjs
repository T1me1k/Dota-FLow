import{loadGolden,updateGolden,runReplaySuite,diffGolden}from'../packages/core/src/replay-calibration.mjs';const update=process.argv.includes('--update');const previous=await loadGolden();const current=update?await updateGolden():Object.fromEntries((await runReplaySuite()).results.map(r=>[r.scenarioId,{checkpoints:r.checkpointResults.map(x=>x.actual),finalCall:r.finalCall}]));if(update){console.log(`Updated ${Object.keys(current).length} explicit golden baselines`)}else{const diffs=[];for(const id of Object.keys(current).sort())for(const d of diffGolden(previous[id],current[id]))diffs.push({scenarioId:id,...d});console.log(diffs.length?JSON.stringify(diffs,null,2):`Golden baselines match (${Object.keys(current).length} scenarios)`);if(diffs.length)process.exitCode=1}
