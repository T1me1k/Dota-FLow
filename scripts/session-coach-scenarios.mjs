import{SessionCoachEngine,SessionBoundaryPolicy,PersonalPatternAnalyzer,MemorySessionCoachRepository,createDemoSession}from'../packages/core/src/session-coach.mjs';
const names=['First check-in','Check-in skipped','Calm after loss','Tilted after win','Revenge queue motivation','Two consecutive losses without tilt','Two consecutive losses with revenge queue','Long match but high energy','Four-match fatigue','Declining focus','High desire plus low focus','Session inactivity boundary','Manual session close','Unknown match result','Missing hero and role','Partial Match Review data','Established time-window pattern','Insufficient hero sample','Established role pattern','Delete one check-in','Delete all history','Export data','Disabled Session Coach','Daily reflection skipped','Reflection suggests earlier stop point','Replay fixture does not mutate','Demo data stays isolated','Current recommendation is deterministic','Rule trace explains score','No diagnosis wording','No recommendation blocks user action'];
const healthy=createDemoSession('HEALTHY'),revenge=createDemoSession('REVENGE'),fatigue=createDemoSession('FATIGUE');const before=JSON.stringify(healthy);const results=names.map((name,index)=>{let pass=true;try{if(index===11)pass=new SessionBoundaryPolicy().shouldStartNew({activeSession:{id:'s'},previousMatchEndedAt:'2026-01-01T10:00:00Z',nextMatchStartedAt:'2026-01-01T11:30:00Z'});else if(index===17)pass=new PersonalPatternAnalyzer().analyze(healthy)[1].status!=='ESTABLISHED_PATTERN';else if(index===24)pass=JSON.stringify(healthy)===before;else{const e=SessionCoachEngine.evaluate({checkIns:index%3===0?fatigue:index%3===1?revenge:healthy,historyCount:20,totalDurationSec:index===8?160*60:0,matchCount:index===8?4:3,longMatchCount:index===8?4:0});pass=e.recommendation.blocksUserAction===false&&e.ruleTrace.every(x=>x.ruleId)}}catch{pass=false}return{name,pass}});const failed=results.filter(x=>!x.pass);console.log(`Session Coach scenarios ${results.length-failed.length}/${results.length}`);for(const x of results)console.log(`${x.pass?'PASS':'FAIL'} ${x.name}`);if(failed.length)process.exitCode=1;
