import{formatPlayerAdvice,validateAdvice}from'../packages/core/src/advice-presentation.mjs';for(const locale of['ru','en'])for(const mode of['BEGINNER','STANDARD','EXPERT']){const text=formatPlayerAdvice({primaryAction:'RESET',confidence:.91,reasonCodes:['LOW_HP','TEAM_NOT_READY','OBJECTIVE_WINDOW_UPCOMING']},{locale,mode});const r=validateAdvice(text,{mode});if(!r.valid)throw new Error(`${locale}/${mode}: ${r.errors}`)}console.log('Advice validation passed: 2 locales × 3 modes');
